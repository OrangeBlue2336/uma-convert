# _partials/

이 폴더의 파일은 여러 페이지가 함께 씁니다. **여기를 고친 뒤에는 저장소 최상위에서
`node build.mjs`를 실행해야** 실제 페이지(`index.html`, `sprite/index.html` 등)에 반영됩니다.

자세한 설명은 저장소 최상위의 `README.md`("커밋하기 전에: node build.mjs")와
`AGENTS.md`, 그리고 `build.mjs` 파일 맨 위 주석을 참고하세요.
